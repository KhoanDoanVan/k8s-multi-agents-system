apiVersion: v1
kind: Service
metadata:
  name: search-agent-service
  namespace: multi-agent
  labels:
    app: search-agent
spec:
  selector:
    app: search-agent
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP